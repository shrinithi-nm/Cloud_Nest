apiVersion: v1
kind: Service
metadata:
  name: cloudnest-frontend-service
spec:
  selector:
    app: cloudnest-frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080